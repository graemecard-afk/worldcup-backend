# Example environment variables for worldcup-backend

# Render will inject this with your Postgres connection string, e.g.
# postgres://user:password@host:5432/dbname
DATABASE_URL=postgres://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_DB_HOST:5432/YOUR_DB_NAME

# Secret key for signing JWT tokens
JWT_SECRET=change-me-to-a-long-random-string

# Port for local dev (Render will set its own PORT)
PORT=4000
